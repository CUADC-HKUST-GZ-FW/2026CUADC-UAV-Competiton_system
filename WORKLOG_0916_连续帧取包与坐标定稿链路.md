# 0916 连续帧取包与坐标定稿链路

日期：2026-09-16

## 1. 今日结论

本轮将飞行侦察的多帧融合从“先按地理坐标半径持续聚类”改为“先按图像时序和像素连续性取包，封包后再做一次坐标融合与包间裁决”。主要目标是避免同一标靶因遥测时序、瞬时分类抖动或异常 Pose 被拆成多个相互竞争的坐标簇。

当前结果：

1. 单标靶飞行链路和比赛三标靶链路均使用新的 `pixel_packets` 模式。
2. 静态定高标定继续使用 `legacy_geo_cluster`，不受飞行封包结束条件影响。
3. `empty` 在地理投影前被丢弃，不进入帧包、坐标融合、比赛选择或飞控桥接。
4. 飞行模式只向下游发布 `valid=true, status=finalized` 的最终坐标。
5. 比赛模式必须得到三个不同的非空 `finalized` 结果，候选签名稳定 1 秒后才发布唯一比赛目标。
6. 飞行模式中的 R95 作为质量描述和同帧数候选的次级排序依据，不是硬拒绝门槛；静态模式仍保留 `R95 <= 0.5 m` 的原有约束。

## 2. 为什么更换旧聚类方式

旧流程直接按逐帧解算经纬度做空间聚类。高速飞行时，遥测对齐误差、边缘投影误差和偶发 Pose 错帧可能把同一实体拆成多个地理簇；之后若只选择其中一个分支，帧数越多不一定越接近真实位置。

新流程把“同一实体的连续视觉轨迹”和“最终地理坐标”分成两个阶段：

```text
识别帧 + 三点 Pose 中心
  -> 像素时序关联
  -> 连续帧包
  -> 包内鲁棒坐标融合与标签多数投票
  -> 包间 1.5 m / 10 m 裁决
  -> finalized 坐标
  -> 单标靶桥接或比赛三目标选择
```

瞬时分类标签不再直接拆包。只要三点中心在时间和像素门限内连续，即使中间少量帧类别抖动，也保留在同一帧包，最终由有效帧多数决定包标签。

## 3. 连续帧取包规则

### 3.1 输入观察

每个有效观察记录：

- 相机采集时间戳；
- 原始帧号和源序号；
- 三点 Pose 中心像素；
- 单帧投影经纬度；
- 分类标签、置信度和 Pose 分数；
- RTK/本地位置传播得到的位置上下文；
- 目标中心到标定主点的归一化距离。

同一帧内的两个检测不能加入同一个帧包，避免重复检测虚增票数。

### 3.2 时间和像素关联

相邻观察必须满足：

```text
0 <= dt <= 0.20 s
pixel_distance <= min(200, 25 + 1300 * dt)
```

若多个活动包都满足，选择“像素距离 / 当前门限”最小的包。无法匹配时创建新包；旧包暂存，等待整组活动结束后统一比较。

### 3.3 封包条件

出现以下任一条件时结束一组帧包：

1. 所有活动包超过 `0.20 s` 没有新观察；
2. 任一活动包达到 `300` 个有效观察。

300 帧在 60 FPS 下约为 5 秒，用来限制静止场景、持续识别或时间异常导致的无限增长。达到上限时，当前活动包和等待包一起进入本轮裁决。

## 4. 包内融合

每个包在融合前和鲁棒离群剔除后都必须至少保留 11 帧，否则丢弃。

坐标融合步骤：

1. 以经纬度中位数建立局部 ENU 原点。
2. 使用残差中位数和 MAD 剔除明显空间异常点。
3. 单帧权重由 `分类置信度 * Pose 分数 * 画面中心权重` 构成。
4. 画面中心权重有下限，不会把边缘帧直接归零，但中心附近帧权重更高。
5. 输出加权经纬度、观测跨度、剔除数量和 R95。

最终标签按鲁棒剔除后的有效帧数量多数投票；票数相同时依次参考累计 `置信度 * Pose 分数` 和稳定标签顺序。用于网页快照和分类信息的代表帧，从最终多数标签内选择质量最高的一帧。

## 5. 包间坐标裁决

一组结束后，对所有达到 11 帧的包按有效帧数优先排序：

| 两包坐标距离 | 处理方式 |
| --- | --- |
| `<= 1.5 m` | 按有效帧数合并坐标，并重新统计全部标签票数 |
| `> 1.5 m` 且 `< 10 m` | 保留帧数更多的包，丢弃较小包 |
| `>= 10 m` | 视为两个不同实体，分别保留 |

有效帧数相同时，R95 和置信度只参与候选排序，不形成额外硬门槛。

## 6. 下游发布与比赛选择

### 6.1 单标靶链路

`recon_geolocator` 只发布封包后的 `finalized` 坐标。`vision_target_bridge` 只锁存第一个合法 `finalized`，随后发布一个 `/vision/target_command`；后续结果不再替换已锁定坐标。

### 6.2 比赛三标靶链路

飞行比赛入口执行以下门槛：

1. 只接受 `finalized`；
2. 排除 `empty` 和无比赛值标签；
3. 空间去重后，同一标签只保留质量最好的代表；
4. 必须凑齐三个不同的非空目标；
5. 三目标候选签名连续稳定 `1.0 s`；
6. 数字模式取三个数字的中位数，图案模式取比赛值最高者；
7. 最终只向桥接器发布一个 `finalized` 目标。

静态定高模式没有帧包结束语义，因此通过显式 `--allow-confirmed` 兼容旧聚类输出。该例外只由静态入口添加，飞行比赛入口仍严格要求 `finalized`。

## 7. 静态模式隔离

以下静态路径显式使用 `tracking_mode=legacy_geo_cluster`：

- `recon_static_with_mavros.launch.py`；
- `competition_selected_static.sh -> competition_selected.sh` 的静态分支。

这样固定高度、固定机位下的目标可以持续更新，仍保留 0.5 米离散半径门槛，不会因目标长期停留在画面中而等待 300 帧封包。

## 8. 网页与日志

网页继续显示已经落盘的最终目标，不展示 `empty`。新增结果状态中文显示“已定稿”。每个最终结果保存：

- `packet_ids` 和包数量；
- 原始、有效和剔除帧数；
- 标签票数；
- 封包原因；
- 0.20 秒、200 像素和 300 帧参数；
- 1.5 米/10 米包间决策记录；
- 最终坐标、R95 和代表帧路径。

关键日志阶段：

```text
[RECON_PACKET] stage=group_finalized
[RECON_PACKET] stage=coordinate_finalized
[FULLCHAIN] stage=coordinate_published
[FULLCHAIN] stage=manager_received
[FULLCHAIN] stage=goto_requested
[FULLCHAIN] stage=push_verified
[FULLCHAIN] stage=set_current_confirmed
```

## 9. 其他同步修复

1. 更新了当前 6 mm 相机的棋盘格内参与畸变参数：52/100 张有效图，OpenCV RMS 约 0.1371 px。
2. 独立 300 秒录像脚本从视觉配置读取相机序列号，同时支持 GigE 和 USB MVS 枚举，不再写死 NX163 路径或旧 USB 序列号。
3. 录像输出默认使用当前用户的 `~/camera_recordings`，并用独立 GStreamer 进程完成 Jetson H.264 封装。

## 10. 验证结果

- Python 语法编译：通过。
- Shell 启动脚本语法：通过。
- ROS 2 构建：6 个包通过。
- 帧包、遥测、桥接、任务安全和航线构造功能测试：`91 passed`。
- 比赛选择器测试：`10 passed`，覆盖飞行拒绝 `confirmed` 和静态显式兼容 `confirmed`。
- 单标靶完整安全 dry-run：3 个独立 ROS Domain 连续通过。
- dry-run 闭环：12 帧输入 -> 1 个 finalized -> 1 个 TargetCommand -> manager 接收 -> goto 规划。
- dry-run 参数：`dry_run_goto=true`、`allow_mission_upload=false`，没有真实 MAVROS 写入和舵机动作。
- 独立录像脚本在 Jetson 成功导入，并从配置读取相机序列号 `DB1225037`；本轮未占用相机进行实际 300 秒录制。

FCU 包的 38 个功能测试通过；完整 `colcon test` 仍会报告该包原有的格式检查债务，包括一个旧测试文件末尾缺换行和若干中文 docstring 的 pydocstyle 提示。它们不在本次功能改动文件中，也不影响上述运行验证。

## 11. 尚未包含的航线时序补丁

本轮完成的是“视觉帧如何形成唯一 finalized 坐标”，尚未实现按航线进度强制交付：

- 没有在 `current_seq == 4` 时强制结束当前活动包；
- 没有独立的目标接收 ACK 消息，桥接器仍以 MissionManager 进入 `EXECUTING` 作为接收确认；
- `current_seq >= insert_wp_index(5)` 的迟到目标仍会被安全拒绝；
- FCU mission pull 通过后到真正 push 之间仍存在很窄的航点切换竞态。

下一轮应单独设计“航点 4 截止锁存、无目标时明确放弃、上传前最终复核和带 target_id 的接收回执”，不要与本轮帧包融合混成一次不可回退的大改动。

## 12. 回退信息

NX163 本轮关键备份：

```text
/home/nx163/uav_ros2_project/.codex_backups/20260916_pixel_packet_before/
/home/nx163/uav_ros2_project/.codex_backups/20260916_before_majority_vote/
/home/nx163/uav_ros2_project/.codex_backups/20260916_before_packet_cap_300/
/home/nx163/backups/competition_selector_20260916_013634/
/home/nx163/backups/competition_selector_static_compat_20260916/
```

备份、运行日志、录像、构建目录和诊断输出不进入 Git 软件提交。
